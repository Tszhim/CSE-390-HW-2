#ifndef DIRECTION_H
#define DIRECTION_H

/**
 * @brief An enum class declaration for directions.
 * 
 * Use this enum when expressing directional relativity (e.g. which wall from a robot). 
 */
enum class Direction { North, East, South, West };

#endif